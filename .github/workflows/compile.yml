# ... 상단 설정부 동일 ...

      - name: Compile Pure C++ and Lua Source Files
        run: |
          echo "=== 1단계: 루아 C 소스 파일 컴파일 (C 규격으로 독립 빌드) ==="
          emcc -O3 -c *.c -I.

          echo "=== 2단계: OCGCore C++ 소스 파일 컴파일 ==="
          em++ -O3 -std=c++17 -c *.cpp -D"__declspec(x)=" -I.

          echo "=== 3단계: 생성된 오브젝트 파일들 최종 결합 및 웹어셈블리 링크 ==="
          em++ *.o \
            -O3 \
            -s WASM=1 \
            -s EXPORTED_FUNCTIONS="['_create_duel', '_start_duel', '_process', '_new_card', '_set_player_info']" \
            -s EXPORTED_RUNTIME_METHODS="['ccall', 'cwrap']" \
            -o ocgcore.js

# ... 하단 커밋부 동일 ...
